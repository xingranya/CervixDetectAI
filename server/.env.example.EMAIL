# 腾讯云邮件推送服务配置

## 环境变量

在 `server/.env` 文件中添加以下配置：

```bash
# 腾讯云 SES 邮件推送配置
TENCENT_SECRET_ID=your_secret_id_here
TENCENT_SECRET_KEY=your_secret_key_here
TENCENT_SES_REGION=ap-guangzhou
TENCENT_SES_FROM_EMAIL=no-reply@hpvsc.icu

# 邮件模板ID
TEMPLATE_ID_REGISTER=164623
TEMPLATE_ID_RESET_PASSWORD=164624
```

## 配置说明

### 1. 腾讯云访问密钥

**获取方式：**
1. 登录腾讯云控制台：https://console.cloud.tencent.com
2. 点击右上角「账号」→「访问管理」→「API密钥管理」
3. 创建密钥或查看现有密钥
4. 复制 `SecretId` 和 `SecretKey`

**配置项：**
- `TENCENT_SECRET_ID`: 腾讯云 SecretId
- `TENCENT_SECRET_KEY`: 腾讯云 SecretKey

⚠️ **安全提示：** 请勿将密钥提交到 Git 仓库！

---

### 2. SES 服务地域

**可选地域：**
- `ap-guangzhou`: 华南地区（广州）- 推荐
- `ap-hongkong`: 港澳台地区（中国香港）

**配置项：**
- `TENCENT_SES_REGION`: 服务地域（默认：`ap-guangzhou`）

---

### 3. 发信地址

**配置项：**
- `TENCENT_SES_FROM_EMAIL`: 发信邮箱地址

**格式：**
```
no-reply@hpvsc.icu
```

⚠️ **前提条件：** 需要先在腾讯云 SES 控制台配置发信域名和发信地址

---

### 4. 邮件模板ID

**当前已配置模板：**
- 注册验证码模板ID: `164623`
- 重置密码模板ID: `164624`

**配置项：**
- `TEMPLATE_ID_REGISTER`: 注册验证码模板ID
- `TEMPLATE_ID_RESET_PASSWORD`: 重置密码模板ID

⚠️ **注意：** 模板需要在腾讯云控制台创建并审核通过后才能使用

---

## 完整 .env 示例

```bash
# 数据库配置
DB_HOST=localhost
DB_PORT=3306
DB_NAME=cervix_detect_ai
DB_USER=root
DB_PASSWORD=your_password

# JWT密钥
JWT_SECRET=your_jwt_secret_here
JWT_REFRESH_SECRET=your_refresh_secret_here

# 阿里云配置
ALIYUN_ACCESS_KEY_ID=your_aliyun_key
ALIYUN_ACCESS_KEY_SECRET=your_aliyun_secret
ALIYUN_SMS_SIGN_NAME=your_sign_name
ALIYUN_SMS_TEMPLATE_CODE=your_template_code
ALIYUN_CAPTCHA_SCENEID_LOGIN=u1g43fza
ALIYUN_CAPTCHA_SCENEID_VERIFY=1dynwu1h

# 腾讯云 SES 邮件推送配置
TENCENT_SECRET_ID=your_tencent_secret_id
TENCENT_SECRET_KEY=your_tencent_secret_key
TENCENT_SES_REGION=ap-guangzhou
TENCENT_SES_FROM_EMAIL=no-reply@hpvsc.icu

# 邮件模板ID
TEMPLATE_ID_REGISTER=164623
TEMPLATE_ID_RESET_PASSWORD=164624

# 易支付配置
EPAY_PID=your_epay_pid
EPAY_KEY=your_epay_key
EPAY_API_URL=https://pay.hpvsc.icu
EXTERNAL_PORT=26140

# 通义千问配置
QWEN_API_KEY=your_qwen_api_key
QWEN_MODEL=qwen-plus

# 服务器配置
PORT=4000
NODE_ENV=development
```

---

## 验证配置

### 1. 测试邮件发送

```bash
# 启动后端服务
cd server
npm start

# 发送测试邮件（使用 curl）
curl -X POST http://localhost:4000/api/auth/email/send-code \
  -H "Content-Type: application/json" \
  -d '{
    "email": "your-email@example.com",
    "type": "register"
  }'
```

### 2. 检查日志

查看后端日志确认邮件发送状态：
```
[TencentEmailService] 发送邮件成功: { RequestId: 'xxx' }
```

### 3. 检查邮箱

验证是否收到验证码邮件，检查：
- ✅ 邮件内容是否正确显示验证码
- ✅ 邮件样式是否正常
- ✅ 验证码是否为6位数字

---

## 常见问题

### 1. 邮件模板未配置或审核未通过

**错误信息：** `FailedOperation.InvalidTemplateID`

**解决方法：**
1. 登录腾讯云 SES 控制台
2. 检查模板状态是否为「审核通过」
3. 确认模板ID是否正确

---

### 2. 发送频率限制

**错误信息：** `FailedOperation.FrequencyLimit`

**说明：**
- 同一邮箱60秒内只能发送1次
- 每日最多发送10次

**解决方法：**
等待限制解除后重试

---

### 3. 邮箱在黑名单中

**错误信息：** `FailedOperation.EmailAddrInBlacklist`

**解决方法：**
1. 登录腾讯云 SES 控制台
2. 在「黑名单管理」中移除该邮箱

---

### 4. 域名未配置

**错误信息：** `FailedOperation.NotAuthenticatedSender`

**解决方法：**
1. 登录腾讯云 SES 控制台
2. 添加发信域名 `hpvsc.icu`
3. 配置 DNS 记录（SPF/DKIM）
4. 创建发信地址 `no-reply@hpvsc.icu`

---

## 参考文档

- [腾讯云 SES 官方文档](https://cloud.tencent.com/document/product/1288)
- [邮件推送 API 文档](https://cloud.tencent.com/document/product/1288/51062)
- [发送邮件接口](https://cloud.tencent.com/document/product/1288/51034)
